import { IUser } from '@/services/models/user';
import { Drawer } from '@the-monkeys/ui/atoms/drawer';

export default function UpdateUserDrawer({ data }: { data: IUser }) {
  return <Drawer></Drawer>;
}
