const SearchPage = ({
  params,
}: {
  params: {
    query: string;
  };
}) => {
  const searchQuery = params.query;

  return <div className='mx-auto max-w-3xl min-h-screen'></div>;
};

export default SearchPage;
