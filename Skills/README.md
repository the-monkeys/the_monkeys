# Technology Skills Backup

This folder is a local backup of the active skills in `.agents/skills`.

All backed-up skills are stored directly in this folder.

The active copies are installed under `.agents/skills`. Restore a skill by copying `Skills/<skill-name>` back to `.agents/skills/<skill-name>`.

Sources and coverage decisions are recorded in `manifest.md`.
